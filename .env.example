# ============================================================
# FoodBiz — Environment Variables Template
# Copy to .env and fill in real values before running locally.
# NEVER commit .env with real secrets.
# ============================================================

# ---- Postgres (Docker Compose local) -----------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=project_genesis
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ---- Backend settings --------------------------------------
DEMO_MODE=true

# Change these before ANY real deployment
ADMIN_PASSWORD=dev_admin_password_change_me
# JWT_SECRET must be ≥ 32 characters in production
JWT_SECRET=dev_jwt_secret_change_me_at_least_32_chars

# Database URL (Docker Compose uses the postgres service "db")
DB_URL=postgresql+psycopg://postgres:postgres@db:5432/project_genesis

# ---- CORS (comma-separated) --------------------------------
# Leave empty = allows only localhost:3000 and localhost:3001.
# In production, set to your real domains, e.g.:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://admin.yourdomain.com
ALLOWED_ORIGINS=

# ---- Reset DB on startup (dev/CI only) ---------------------
# Set to true to drop and recreate tables each startup.
# NEVER set to true in production.
RESET_DB_ON_STARTUP=true

# ---- Stripe (leave empty to disable Stripe) ----------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=http://localhost:3000/order/success?session_id={CHECKOUT_SESSION_ID}
STRIPE_CANCEL_URL=http://localhost:3000/order/cancel

# ---- Frontend / Admin Next.js ------------------------------
# URL of the backend as seen from the BROWSER (not Docker network).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8010
